# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
.venv
venv/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
coverage.xml
.mypy_cache/

# Frontend
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/coverage/
*.tsbuildinfo

# Environment
# Catch-all rather than a list: `.env`, `.env.local` and `.env.*.local` left siblings like
# `.env.backup` / `.env.a8-backup` untracked but NOT ignored, so they sat in `git status` as `??`
# holding live GEMINI_API_KEY / ANTHROPIC_API_KEY values — one `git add -A` away from committing
# real secrets to a public repo. That is RSK-08 / KI-03's exact scenario, and a naming convention
# is not a control. The `!` lines keep the templates a clean clone needs.
.env
.env.*
!.env.example
!.env.*.example

# OS
.DS_Store
Thumbs.db
*/worktrees/

# Ticket-verification screenshots. CONTRIBUTING.md puts screenshots in the documentation archive,
# not here, but they land at the repo root and otherwise sit in `git status` indefinitely — one
# `git add -A` away from being committed. Anchored to the root so frontend/acra_logo.png is
# unaffected.
/*.png

# Validation harness output (scripts/validation-run.sh)
validation-evidence/

# Ticket plan files (plans/plan_ticket_N.md) are review scratch space, deleted once a ticket
# ships — see CONTRIBUTING.md. Files already committed before this rule (plans/plan_ticket_*.md,
# plans/plan_a8_a10_readiness.md) are unaffected: an ignore rule never untracks a tracked file.
plans/

# Playwright (e2e specs are tracked; run output is not)
frontend/test-results/
frontend/playwright-report/
frontend/blob-report/
.playwright-mcp/
